export enum EuroKeyValues {
	NOT_SELECTED = -1,
	YES = 1,
	NO = 0,
}

export enum DoorWidths {
	NOT_SELECTED = -1,
	DOOR_WIDTH_80 = 80,
	DOOR_WIDTH_90 = 90,
	DOOR_WIDTH_100 = 100,
	DOOR_WIDTH_110 = 110,
}

export enum RampValues {
	NOT_SELECTED = -1,
	NO_RAMP = 0,
	ACCESSIBLE_RAMP = 6,
}
